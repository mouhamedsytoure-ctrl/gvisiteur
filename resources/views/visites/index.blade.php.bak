@extends('layouts.app')

@section('title', 'Visites – Gestion')

@section('content')
<div class="container-fluid py-4">

  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h2 class="mb-0" style="font-size:1.6rem; font-weight:800; color:#111827;">Enregistrement des Visites</h2>
      <p class="text-muted mb-0 small">Suivi et gestion des arrivées / départs</p>
    </div>

    <div>
      <a href="{{ route('visites.create') }}" class="btn btn-accent">+ Nouvelle visite</a>
    </div>
  </div>

  <div class="row g-3">
    <!-- Form (optionnel compact) -->
    <div class="col-12 col-lg-4">
      <div class="card card-dark p-3">
        <h6 class="mb-2" style="font-weight:600">Ajouter une nouvelle visite</h6>
        <p class="small-muted mb-3">Formulaire rapide</p>
        <form action="{{ route('visites.store') }}" method="POST" autocomplete="off">
          @csrf
          <div class="mb-2">
            <input name="nom_visiteur" value="{{ old('nom_visiteur') }}" class="form-control" placeholder="Nom du Visiteur" required>
          </div>
          <div class="mb-2">
            <input name="personne_rencontree" value="{{ old('personne_rencontree') }}" class="form-control" placeholder="Personne rencontrée" required>
          </div>
          <div class="mb-2 d-flex gap-2">
            <input name="date_arrivee" type="datetime-local" class="form-control" value="{{ old('date_arrivee') }}">
          </div>
          <div class="d-flex gap-2">
            <button class="btn btn-accent" type="submit">Enregistrer l'Arrivée</button>
            <a href="{{ route('visites.index') }}" class="btn btn-outline-light">Annuler</a>
          </div>
        </form>
      </div>
    </div>

    <!-- Liste -->
    <div class="col-12 col-lg-8">
      <div class="card card-dark p-3">
        <div class="d-flex justify-content-between align-items-center mb-3">
          <div>
            <h6 class="mb-0" style="font-weight:600">Historique des Visites Récentes</h6>
            <p class="small-muted mb-0">Dernières entrées</p>
          </div>

          <div class="d-flex gap-2 align-items-center">
            <input id="search" type="text" class="form-control form-control-sm" placeholder="Rechercher un visiteur..." style="min-width:220px">
          </div>
        </div>

        <div class="table-responsive">
          <table class="table table-borderless table-sm align-middle mb-0">
            <thead>
              <tr class="small-muted">
                <th>Client</th>
                <th>Visiteur</th>
                <th>Arrivée</th>
                <th>Départ</th>
                <th>Statut</th>
                <th class="text-end">Action</th>
              </tr>
            </thead>

            <tbody id="visits-table">
              @forelse($visites as $visite)
                <tr>
                  <td>{{ optional($visite->client)->nom ?? '—' }}</td>

                  <td>
                    {{-- Le nom est cliquable pour éditer --}}
                    <a href="{{ route('visites.edit', $visite->id) }}" class="text-decoration-none fw-medium">
                      {{ $visite->nom_visiteur ?? (optional($visite->client)->nom ?? '—') }}
                    </a>
                    <div class="small text-muted">{{ \Illuminate\Support\Str::limit($visite->motif ?? '-', 40) }}</div>
                  </td>

                  <td>{{ optional($visite->date_arrivee)->format('d/m/Y H:i') ?? '—' }}</td>
                  <td>{{ optional($visite->date_sortie)->format('d/m/Y H:i') ?? '-' }}</td>
                  <td>
                    @php $s = strtoupper($visite->statut ?? '') @endphp
                    @if($s === 'TERMINEE')
                      <span class="badge-soft badge-soft-success">Terminée</span>
                    @elseif($s === 'EN_COURS')
                      <span class="badge-soft badge-soft-warning">En cours</span>
                    @else
                      <span class="badge-soft badge-soft-secondary">{{ $s ?: 'N/A' }}</span>
                    @endif
                  </td>

                  <td class="text-end">
                    <div class="d-flex justify-content-end align-items-center gap-2">
                      <a href="{{ route('visites.edit', $visite->id) }}" class="btn btn-sm btn-outline-dark">Éditer</a>

                      {{-- Formulaire de suppression --}}
                      <form action="{{ route('visites.destroy', $visite->id) }}" method="POST" onsubmit="return confirmDelete(event, this)" class="m-0">
                        @csrf
                        @method('DELETE')
                        <button type="submit" class="btn btn-sm btn-outline-danger">Supprimer</button>
                      </form>
                    </div>
                  </td>
                </tr>
              @empty
                <tr>
                  <td colspan="6" class="text-center small-muted py-4">Aucune visite enregistrée.</td>
                </tr>
              @endforelse
            </tbody>
          </table>
        </div>

      </div>
    </div>
  </div>
</div>

<script>
  // Recherche simple côté client (filtre texte)
  document.addEventListener('DOMContentLoaded', function () {
    const search = document.getElementById('search');
    const rows = Array.from(document.querySelectorAll('#visits-table tr'));

    search?.addEventListener('input', function () {
      const q = this.value.trim().toLowerCase();
      rows.forEach(r => {
        const text = r.innerText.toLowerCase();
        r.style.display = text.includes(q) ? '' : 'none';
      });
    });
  });

  // Confirmation avant suppression
  function confirmDelete(ev, form) {
    ev.preventDefault();
    if (confirm('Confirmer la suppression de cette visite ?')) {
      form.submit();
    }
    return false;
  }
</script>
@endsection